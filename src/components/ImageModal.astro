---

//    CSS & STYLE IMPORTS

//    ASTRO COMPONENT IMPORTS

//    ASTRO LAYOUT IMPORTS 

//    ASTRO PAGE-SPECIFIC IMPORTS 

//    FRAMEWORK-SPECIFIC IMPORTS 

//    COMPONENT SCRIPTS:

const { imgSource, imgAlt, imgClass, imgIdThumbnail, imgLinkThumbnail, imgIdFullScreen, imgLinkFullScreen } = Astro.props;

/// You can write any JavaScript/TypeScript that you'd like here.
/// It will run during the build, but never in the browser.
/// All variables are available to use in the HTML template below.

//    PAGE-SPECIFIC VARIABLES

/// FULL ASTRO COMPONENT SYNTAX:
/// https://docs.astro.build/core-concepts/astro-components/

---
<div class="image_modal__container">
  <div class="image_modal__thumbnail">
    <a class="utility_classes__reset image_modal__open" href={imgLinkFullScreen}>
      <img
        src={imgSource}
        alt={imgAlt}
        class={imgClass}
        id={imgIdThumbnail}
        loading="lazy"
      />
    </a>
  </div>
  <div class="image_modal__full_screen_bg" id={imgIdFullScreen}>
    <div class="image_modal__full_screen">
      <a href={imgLinkThumbnail} class="utility_classes__reset image_modal__close"></a>
      <img
        src={imgSource}
        alt={imgAlt}
        id={imgLinkFullScreen}
        loading="lazy"
      />
    </div>
  </div>
</div>
